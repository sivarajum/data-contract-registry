[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --tb=short --cov=src --cov-fail-under=80 --cov-config=.coveragerc
markers =
    integration: marks tests that require live GCP credentials (deselect with -m "not integration")
